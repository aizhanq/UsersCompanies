@model IEnumerable<UsersCompanies.Web.Models.UserViewModel>

@{
    ViewBag.Title = "Users";
}

<h2>Users</h2>

<p>
    <a asp-action="CreateUser">Create New</a>
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Users", "GetUsersByCompanyId", new { id = item.Id })
                @Html.ActionLink("Jobs", "GetJobsByCompanyId", new { id = item.Id })
            </td>
            <td>
                @Html.ActionLink("Details", "GetUsersByCompanyId", new { id = item.Id })
                @Html.ActionLink("Edit", "EditCompany", new { id = item.Id })
                @Html.ActionLink("Delete", "DeleteCompany", new { id = item.Id })
            </td>
        </tr>
    }

</table>